# Postgres Live
API_SECRET=20RVSE19
DB_HOST=localhost            # when running the app without docker 
#DB_HOST=fullstack-postgres    # when running the app with docker 
DB_DRIVER=postgres
DB_USER=postgres
DB_PASSWORD=Diego1587
DB_NAME=redcoins
DB_PORT=5432 #Default postgres port

# Used by pgadmin service 
PGADMIN_DEFAULT_EMAIL=live@admin.com
PGADMIN_DEFAULT_PASSWORD=password

# Postgres Test
TestApiSecret=20RVSE19
# TestDbHost=postgres_test 
TestDbHost=localhost    # when running the app without docker 
TestDbDriver=postgres
TestDbUser=postgres
TestDbPassword=Diego1587
TestDbName=redcoins_test
TestDbPort=5432